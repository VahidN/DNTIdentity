@model RegisterEmailConfirmationViewModel
@{
    Layout = "~/Areas/Identity/Views/EmailTemplates/_EmailsLayout.cshtml";
}
&nbsp; با سلام
<br />
&nbsp; اکانت شما با مشخصات ذیل ایجاد گردید:
<br />
<br />
<div align="center" dir="rtl" style="font-family: Tahoma; font-size: 9pt">
    نام کاربری (جهت لاگین) : @Model.User.UserName
    <br />
    نام مستعار (جهت نمایش در سایت) : @Model.User.DisplayName
    <br />
    <br />
    لطفا جهت فعال سازی اکانت خود بر روی لینک ذیل کلیک نمائید:
    <br />
    <div align="center">
        <a href="@Url.Action("ConfirmEmail", "Register", values: new { userId = Model.User.Id, code = Model.EmailConfirmationToken, area = AreaConstants.IdentityArea }, protocol: this.Context.Request.Scheme)">
            فعال سازی اکانت
        </a>
    </div>
    <br />
    <br />
    همچنین در هر زمان برای بازیابی کلمه عبور فراموش شده خود می‌توانید از آدرس ذیل استفاده
    نمائید:
    <br />
    <div align="center">
        <a href="@Url.Action("Index","ForgotPassword", values: new { area = AreaConstants.IdentityArea }, protocol: this.Context.Request.Scheme)">
            بازیابی
            کلمه عبور
        </a>
    </div>
</div>
<br />
<br />
&nbsp;با احترام<br />
&nbsp;<a style="direction:ltr" href="@Url.Action("Index", "Home", values: new { area = "" }, protocol: this.Context.Request.Scheme)">@Model.EmailSignature</a>

<br />
<span lang="fa" dir="ltr">
    &nbsp;&nbsp;
    @Model.MessageDateTime
</span>
<br />
